import { Fragment } from 'react';

export default () => (
    <Fragment>
        <div uk-spinner="true"
            style={{ margin: 'auto', display: 'block' }}
        >Spinner</div>
    </Fragment>
);